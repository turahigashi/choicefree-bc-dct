.lake/
build/
*.olean
*.ilean
*.log.lock
.env
logs/build_audit.rerun.txt
logs/static_audit.rerun.txt
logs/reading_layer_axioms.rerun.txt
logs/public_surface.rerun.txt
logs/mathdemo_build.rerun.txt
.lake/reading_layer_axioms_check.lean
paper/*.aux
paper/*.log
paper/*.out

# Python bytecode from running the audit tools
__pycache__/
*.pyc
logs/vacuous_statements.rerun.txt
logs/reachable_core.rerun.txt
logs/vacuous.err

# The log-generation check writes its verdict to its own file rather than into the
# run log; it is a check *on* the shipped reference logs, not one of them.
logs/log_generation.rerun.txt
logs/log_generation.txt

# The JAR pre-submission review bundle is kept here for convenience.  It carries the
# paper PDFs and is not part of the software deposit.
jar-submit-check.zip
